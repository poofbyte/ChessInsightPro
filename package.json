{
  "name": "chessinsightpro-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "setup-engines": "node scripts/setup-engines.js",
    "postinstall": "node scripts/setup-engines.js"
  },
  "devDependencies": {
    "stockfish": "18.0.8",
    "typescript": "^5.7.2"
  }
}

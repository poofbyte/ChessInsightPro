import React from "react";
import GenericPlaceholder from "../placeholder";

export default function EngineDashboard() {
  return <GenericPlaceholder title="Engine Health" description="Stockfish server load, average analysis duration, and WASM memory usage." />;
}

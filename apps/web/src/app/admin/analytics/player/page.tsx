import React from "react";
import GenericPlaceholder from "../placeholder";

export default function PlayerDashboard() {
  return <GenericPlaceholder title="Player Intelligence" description="ELO rating distributions across users, and aggregated weakness detection." />;
}
